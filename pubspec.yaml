name: tbib_phone_form_field
description: This library work with phone_form_field.
version: 0.0.3
homepage: https://github.com/the-best-is-best/tbib_phone_form_field

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations: # Add this line
    sdk: flutter

  circle_flags: ^0.0.3
  phone_numbers_parser: ^5.0.0
  dart_countries: ^3.0.2
  intl: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0